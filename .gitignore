__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
benchmarks/_cache/
gpl_src.py

# 코퍼스 수집 — 내려받은 카피레프트 코드(캐시)와 빌드된 지문 DB
# 캐시엔 GPL 원본이 들어가므로 절대 커밋 금지(§4). DB는 지문만이라 안전하지만 산출물이므로 무시.
src/provenire/index/_corpus_cache/
*.db
# 단, 배포용 지문 인덱스는 커밋한다 — 지문 해시 + 라이선스 메타뿐이라 원본 복원이
# 불가능하다(저작권 안전). GitHub Action 이 이 파일을 --index 로 쓴다.
!data/copyleft.db

# 팀장 전용 (커밋 금지 — 로컬에서만 본다)
_ops/

# 비밀 정보
.env
.env.*
*.pem
*.key
*.p12
credentials.json
